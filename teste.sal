module Triangulo;
globals
   a,b,c: int;
proc main()
start
   scan(a); scan(b); scan(c);
   if (a+b < c v a+c < b v b+c < a)
      print(0);
   else
      if (a = b ^ b = c)
         print(1);
      else
         if ( a <> b ^ b <> c)
            print(3);
         else
            print(2);
end